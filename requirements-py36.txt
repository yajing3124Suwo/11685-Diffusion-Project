# Python 3.6.8 (last torch line with 3.6 support: 1.10.x)
# Pin versions compatible with macOS/Linux CPU/GPU; adjust torch wheel URL for your platform if needed.
numpy>=1.19.0,<1.20
Pillow>=8.0.0
tqdm>=4.62.0
ruamel.yaml>=0.17.0
torch==1.10.2
torchvision==0.11.3
# Optional:
# wandb
