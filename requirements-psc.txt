# Install AFTER loading a Bridges-2 AI/pytorch module and `source activate $AI_ENV`.
# PyTorch and torchvision come from the PSC module — do not pip install torch here.
ruamel.yaml>=0.17.0
tqdm>=4.62.0
Pillow>=8.0.0
numpy>=1.19.0
